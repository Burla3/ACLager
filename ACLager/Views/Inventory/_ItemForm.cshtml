@using ACLager.CustomClasses
@using ACLager.Models
@model ACLager.ViewModels.InventoryViewModel

<div class="form-group label-floating">
    <label class="control-label" for="amount">Mængde</label>
    @Html.EditorFor(model => model.Item.Amount, new {htmlAttributes = new {@class = "form-control"}})
    @Html.ValidationMessageFor(model => model.Item.Amount, "", new {@class = "text-danger"})
</div>

<div class="form-group label-floating">
    <label class="control-label" for="supplier">Leverandør</label>
    @Html.EditorFor(model => model.Item.Supplier, new { htmlAttributes = new { @class = "form-control" } })
    @Html.ValidationMessageFor(model => model.Item.Supplier, "", new { @class = "text-danger" })
</div>

<div class="form-group">
    @*<label for="selectLocation" class="control-label">Location</label>*@
    @Html.LabelFor(model => model.Item.Location.UID, new {@class = "form-control"})
    @Html.DropDownListFor(model => model.Item.Location.UID, Model.Locations, new {@class = "form-control chosen-select"})
</div>

<div class="form-group">
    <label for="selectLocation" class="control-label">Location</label>
    @Html.DropDownListFor(model => model.Item.ItemType.UID, Model.ItemTypes, new { @class = "form-control chosen-select" })
</div>


@*<div class="form-group">
        <label for="selectItemType" class="control-label">Varetype</label>
        <select id="selectItemType" class="form-control chosen-select" data-placeholder="Vælg en varetype fra listen. Skriv for at søge">
            <option disabled selected value> -- Vælg en varetype -- </option>
            @foreach (ItemGroup itemGroup in Model.ItemGroups) {
                <option value="@itemGroup.ItemType.UID">@itemGroup.ItemType.Name</option>
            }
        </select>
    </div>
*@

