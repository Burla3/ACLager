@using ACLager.Models
@model ACLager.ViewModels.WasteViewModel

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="container" id="wasteSort">
    @Html.Partial("_SearchPanel")
</div>

<div class="container">
    <div class="row">

        <div class="sort noselect col-sm-3 text-uppercase text-center" data-sort="sort-date">
            Dato
            <br>
            <span class="label label-default">Klik for at sortere</span>
            <span class="label label-primary asc">Sorteret stigende</span>
            <span class="label label-primary desc">Sorteret faldende</span>
            <br>
        </div>

        <div class="col-sm-3">
            Produkt spildt
        </div>

        <div class="col-sm-3">
            Spildrapport ID
        </div>

        <div class="col-sm-3"></div>
    </div>
</div>

<div class="container list" id="ExpansionPanelsContainer">
    @foreach (WasteReport wasteReport in Model.WasteReports) {
        <div class="expansionpanel row panel">
            <div class="row">
                <div class="col-sm-3 sort-date">
                    <h5>
                        <strong>@wasteReport.Date</strong>
                    </h5>
                </div>
                <div class="col-sm-3 sort-name">
                    @*wasteReport.Item.ItemType.Name*@
                </div>
                <div class="col-sm-2 sort-uid">
                    <strong>@wasteReport.UID</strong>
                </div>
                <div class="col-sm-2">

                </div>
                <div class="expansion-control col-sm-2" data-expandmaxheight="100px" data-expandtarget="#ExpansionContentContainer_@wasteReport.UID">
                    <div class="btn btn-default pull-right">
                        <i class="material-icons">expand_more</i>
                        <i class="material-icons hidden">expand_less</i>
                    </div>
                </div>
            </div>
            <div class="row expansion-content" id="ExpansionContentContainer_@wasteReport.UID">
                <div class="col-sm-2">
                    <h5>Mængde mistet: @wasteReport.Amount</h5>
                </div>
                <div class="col-sm-2">
                    @* <h5>Oprettet af: @wasteReport.User.Name</h5>*@
                </div>
                <div class="col-sm-3">
                </div>
                <div class="col-sm-5">
                    @*
                        <a href="@Url.Action("Detailed", new {id = wasteReport.UID})" class="btn btn-raised btn-primary">Mere info</a>
                        <a href="@Url.Action("EditUser", new {id = wasteReport.UID})" class="btn btn-raised btn-info">Rediger</a>
                    *@
                </div>
            </div>
        </div>
    }
</div>

<script type="text/javascript">
    $(document).ready(function () {

        var options = {
            valueNames: ['sort-date']
        };
        var userList = new List('wasteSort', options);
    });
</script>
